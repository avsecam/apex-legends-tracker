{
  "name": "avsecam-apex-tracker",
  "version": "0.1.0",
  "homepage": "https://avsecam.github.io/apex-legends-tracker",
  "engines": {
    "node": "14.17.3"
  },
  "private": true,
  "dependencies": {
    "axios": "^0.26.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "json-loader": "^0.5.7",
    "nodemon": "^2.0.15"
  },
  "scripts": {
    "start-backend": "nodemon ./server/server.js",
    "start-frontend": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
